import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "my5starreport.com | Skilled Nursing 5-Star Analysis Tool",
  description: "The industry expert on 5-star ratings for skilled nursing facilities. Analyze CMS data, improve ratings, and access training resources.",
  keywords: "nursing home, 5-star rating, CMS, skilled nursing, HPRD, PBJ, quality measures, health inspections",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
